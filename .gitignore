node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Capacitor native project (in CI geregenereerd via cap add)
android/
ios/

# Lokale preview-render (niet nodig in repo)
preview/
preview-dist/
vite.preview.config.ts
android-signing/
